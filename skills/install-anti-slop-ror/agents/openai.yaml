interface:
  display_name: "Anti-Slop Rails Review"
  short_description: "Review Rails diffs for AI slop, or install the cops"
  default_prompt: "Use $install-anti-slop-ror to review this Rails PR for AI-agent slop, or to install the anti-slop cops in this repo — reviewing doesn't require installing first."
