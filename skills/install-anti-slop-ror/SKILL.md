---
name: install-anti-slop-ror
description: Vendor the Anti Slop Rails cops into a Rails repository.
---

Run `ruby scripts/install.rb TARGET`. The copied plugin is a snapshot: rerun the installer to update it. It does not edit the target project; add the printed StandardRB plugin block, then run its printed blocking verification command.
